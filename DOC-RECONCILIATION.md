# DOC-RECONCILIATION

This file was generated by the `/doc-truth-up` documentation-reconciliation pass,
which treats the code as read-only ground truth and edits only documentation so it
reflects the repo's actual current state. No code was changed and no builds or tests
were run. It exists so a reviewer can quickly audit what changed and why.

---

## Claim 1 — What it is

**Status: drifted → fixed (CLAUDE.md Architecture section)**
**Evidence: verified-by-reading-code**

README and CLAUDE.md accurately describe the project as a browser-based orbital
mechanics puzzle game with Verlet integration, Kepler solver, patched conics,
30 hand-authored levels across 4 acts, and Sandbox mode. All confirmed by source
files (src/simulation/VerletSimulation.ts, KeplerSolver.ts, PatchedConics.ts,
src/levels/ with 30 JSON files across acts 1–4, src/game/SandboxManager.ts).

One drift: CLAUDE.md Architecture listed `Level editor: ?devtools=1 URL param
(authoring only, not exposed to players)`. There is no `src/devtools/` directory
and no `devtools` string anywhere under `src/`. The level editor was planned in
IMPLEMENTATION-ROADMAP.md Phase 1 Task 10 but was never implemented.

Additionally, the Architecture section listed `Viewport.ts` and `GameLoop.ts`
without path prefixes, making them appear to be at the project root. Both files
live in subdirectories (`src/renderer/Viewport.ts` and `src/game/GameLoop.ts`).

Changes:
- `CLAUDE.md` Architecture: removed `Level editor: ?devtools=1 URL param` bullet
  (no implementation backing the claim).
- `CLAUDE.md` Architecture: `Viewport.ts` → `src/renderer/Viewport.ts`;
  `GameLoop.ts` → `src/game/GameLoop.ts` (added correct paths).

---

## Claim 2 — Current state

**Status: drifted → fixed (docs/PORTFOLIO-DISPOSITION.md)**
**Evidence: verified-by-reading-code (git log)**

CLAUDE.md portfolio-context "Current State" already reads "v1.0 complete — all 4
phases shipped. 30 levels across 4 acts, Sandbox mode, Vercel deploy." — consistent
with code. All simulation, game, renderer, and UI modules are implemented and all
30 level JSONs exist.

docs/PORTFOLIO-DISPOSITION.md "Last known reference" table had a stale tip SHA
(`081de9c Merge feat/vercel-analytics`) from the previous truth-up pass. Three
commits have landed since: `f9d7106` (docs: refresh Codex communication contract),
`b5dbc5b` (docs: lean CLAUDE.md (claude-md-lint)), and the current HEAD.

Change:
- `docs/PORTFOLIO-DISPOSITION.md` Last known reference table: `origin/main tip`
  `081de9c Merge feat/vercel-analytics: @vercel/analytics via inject()`
  → `b5dbc5b docs: lean CLAUDE.md (claude-md-lint)`.

Note: The "Verification posture" section still shows `07e4d12` as the tip with a
chain of production-hardening commits. This section is a historical snapshot from
the original disposition analysis; updating the tip SHA alone would make the section
internally inconsistent (tip SHA would not match the listed commit message).
Left unchanged; flagged for operator review if the section needs refreshing.

---

## Claim 3 — Stack

**Status: consistent**
**Evidence: verified-by-reading-code (package.json)**

README lists TypeScript 5.7, Vite 6, Vitest 3. CLAUDE.md lists TypeScript 5.7+,
Vite 6+, Vitest 3+. package.json confirms: `"typescript": "~5.7.0"`,
`"vite": "^6.0.0"`, `"vitest": "^3.0.0"`. No changes made.

Note for manual review: `vercel.json` specifies `"buildCommand": "pnpm build"`,
but all documentation instructs `npm run build` / `npm install`. There is no
`pnpm-lock.yaml` in the repo. This is a code/config discrepancy (not a docs drift
the doc-truthing pass can fix), but it may cause Vercel deploy failures if pnpm is
not pre-installed in the Vercel build environment. Flagged below.

---

## Claim 4 — How to run

**Status: consistent**
**Evidence: verified-by-reading-code (package.json scripts)**

README documents `npm install`, `npm run dev`, `npm test`, `npm run build`,
`npm run preview`. CLAUDE.md Build/Test/Run section matches. All five script names
exist verbatim in package.json `scripts` block. No changes made.

---

## Claim 5 — Known risks / dependency posture

**Status: consistent**
**Evidence: verified-by-reading-code (package.json:19-22, src/main.ts:1,5)**

README: "Minimal dependencies — @vercel/analytics is the only runtime package; no
game engine, physics library, or UI framework." CLAUDE.md: "@vercel/analytics only
— no game engine, UI framework, or physics library in bundle." docs/PORTFOLIO-DISPOSITION.md:
"One runtime dependency (@vercel/analytics for usage tracking) beyond Canvas 2D."
All consistent with package.json `dependencies: { "@vercel/analytics": "2.0.1" }`
and src/main.ts `import { inject } from "@vercel/analytics"; inject();`.
No changes made.

---

## Claim 6 — Next move (demonstrably incomplete items)

**Status: consistent**
**Evidence: verified-by-reading-code**

Phase 4 verification checklists in IMPLEMENTATION-ROADMAP.md contain unchecked
items (Lighthouse audit, full 30-level production playthrough, etc.) that are
appropriate operational sign-off items. No change needed.

---

## Contradictions for manual review

These items are in files outside the editable doc list
(`README.md` / `CLAUDE.md` / `AGENTS.md` / `DOC-RECONCILIATION.md` / `docs/`),
so they cannot be fixed by this pass.

**IMPLEMENTATION-ROADMAP.md — `src/devtools/LevelEditor.ts` (Phase 1 Task 10)**
That file was never created; the `src/devtools/` directory does not exist and
there are no devtools-related references in `src/`. The roadmap text describes
it as a to-do item, so it was planned but not implemented.

**IMPLEMENTATION-ROADMAP.md — File structure section**
Lists `src/renderer/UIRenderer.ts`, `src/ui/components/StarRating.ts`, and
`src/ui/components/TimeControl.ts`. None exist. The implementation used
`src/ui/ScreenManager.ts` and `src/ui/screens/` instead (ResultOverlay.ts,
Settings.ts, etc.).

**IMPLEMENTATION-ROADMAP.md — Testing Reference table**
Lists `tests/simulation/LevelConfig.test.ts` (JSON schema validation for all 30
level files). This file does not exist. The test was planned but never implemented.

**IMPLEMENTATION-ROADMAP.md — "No analytics" in Security section (~line 323)**
States "No credentials. No server. No analytics." @vercel/analytics is present.
Suggested fix: "No credentials. No backend server. Analytics: @vercel/analytics
injects a lightweight page-view tracker via `inject()` in `main.ts`."

**IMPLEMENTATION-ROADMAP.md — Dependencies scaffold block (Phase 0)**
Shows `npm install -D vitest@1.6 @vitest/ui@1.6`. Actual: `vitest@^3.0.0` and no
`@vitest/ui`. Stale example from before version bumps.

**vercel.json — `"buildCommand": "pnpm build"` (line 2)**
All documentation says `npm run build` / `npm install`. There is no `pnpm-lock.yaml`.
Using pnpm in the Vercel buildCommand while the repo is an npm project may cause
Vercel deployment failures. Suggested fix: change `"buildCommand": "npm run build"`
or add a `pnpm-lock.yaml` and document pnpm as the package manager.

**docs/PORTFOLIO-DISPOSITION.md — "Verification posture" section tip**
Still shows `07e4d12` as `origin/main` tip. Current HEAD is `b5dbc5b`. This section
is a historical snapshot and updating the tip alone would create internal
inconsistency with the listed commit chain. Operator should decide whether to
refresh the whole section or leave it as a historical artifact.

**AGENTS.md — "no telemetry" rule**
Core Rules state "Keep state local-only; do not add backend storage, accounts,
telemetry, or sync." @vercel/analytics sends lightweight page-view data to Vercel.
Whether this lightweight deploy-analytics counts as "telemetry" in the spirit of
the rule is a judgment call for the operator; AGENTS.md text left unchanged.

---

## Footer

Generated: 2026-06-02 19:37:34 PDT
Branch: docs/truth-up-2026-06-02
HEAD reconciled against: b5dbc5b91b0a9091f2c06ccaf79498c6e658dc78
